question
Write a createOddArray function that takes a non-negative integer n as an argument and returns an array containing n odd numbers.
Example:
the createOddArray (5) function should return [1, 3, 5, 7, 9];
the createOddArray (2) function must return [1, 3];
the createOddArray (1) function must return [1];
createOddArray (0) function should return []


solution:

function createOddArray(n){
  let arr = [];
    for(let i = 0; i < (n*2); i++){
      if(i % 2 !== 0) arr.push(i);
    }
    return arr;  
}
