question
Write a createNegativeArray function that takes a non-negative integer n as an argument and returns an array containing n negative numbers 
between -1 and -n.
Example:
the createNegativeArray (5) function should return [-1, -2, -3, -4, -5];
createNegativeArray (2) should return [-1, -2];
the createNegativeArray (1) function must return [-1];
the createNegativeArray (0) function must return [].



 solution :
 
 function createNegativeArray(n){
  let arr = [];
    for(let i = 1; i <= n; i++){
    arr.push(-i);
    }
    return arr;
}
