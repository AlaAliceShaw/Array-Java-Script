question;
Write a function addValue that takes an array arr and value as arguments, adds value to the end of the array, 
and returns the resulting array.
Example:
the addValue ([1, 2, 3], 10) function should return [1, 2, 3, 10];
addValue ([1, 1, 1], "a") should return [1, 1, 1, "a"];
function addValue ([], 10) should return [10]

solution:

function addValue(arr, value){
 let res = arr.push(value);
   for(let i = 0 ; i < arr.length; i++){
   }
  return arr;
}
